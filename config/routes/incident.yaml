# Incidents module screens (uhifadhilabs/incident-module) — the dashboard,
# library, case files and the report flow, all nested under their area:
# /areas/{uuid}/modules/incidents/…
incident:
    resource: '@UhifadhiLabsIncidentBundle/src/Controller/'
    type: attribute
