# Patrols module (uhifadhilabs/patrol-module). dev_tools registers the
# patrol:seed:* commands — dev/test only, so production never grows commands
# that fabricate patrols.
when@dev:
    patrol:
        dev_tools: true

when@test:
    patrol:
        dev_tools: true
